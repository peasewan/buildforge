# Six-class expansion — approved execution plan

Scope: Rogue, Priest, Druid, Warlock, Hunter and Shaman; 15 intent-specific page definitions each. Reuse the class framework, keep existing published URLs and metadata stable. User authorized planning, implementation, tests and production deployment without additional approval, and requested GSC sitemap resubmission after release.

1. Import versioned client Talent/TalentTab/SpellName records and archive public source snapshots. Resolve coordinates, rank caps and prerequisites with primary client tables. Preserve field-level evidence and unresolved tooltip gaps; never manufacture rank text.
2. Build six class datasets and editorial Level-20 allocations. Validate every point through the existing engine. Distinguish client structure from point-budget and editorial assumptions.
3. Add 90 distinct page definitions, shared supporting intents (pet/healing/tank/totem), comparisons, usable calculator deep links, internal links, and per-page publish gates.
4. Generate six class-specific hero images with head-safe framing, download referenced talent icons, and reuse the existing design system.
5. Test import conflicts, dataset identities/prerequisites, allocation legality, page uniqueness, publish gates, asset existence, sharing and rendered page content. Run lint, TypeScript, full test suite and production build. Verify old metadata remains stable.
6. Commit, merge to main and push. Verify the deployed routes, canonical tags and sitemap. Resubmit https://buildforgetools.com/sitemap.xml through GSC MCP and report its actual returned status (submission is not an indexing guarantee).

No watcher restart. No fabricated talent values, popularity statistics or claims of optimal builds.
